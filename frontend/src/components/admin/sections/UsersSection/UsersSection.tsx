import styles from "./UsersSection.module.scss";
import UsersSectionProps from "./UsersSection.props";

const UsersSection = (props: UsersSectionProps): JSX.Element => {
    return (
        <div>

        </div>
    );
};

export default UsersSection;
